import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Use ReactDOM to render the App Component from the root of index.html
ReactDOM.render(<App/>, document.getElementById('root'));
